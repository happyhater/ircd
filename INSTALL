DEBIAN VERSION:

apt-get install build-essential libpcre3 libpcre3-dev libssl-dev gcc byacc flex bison autoconf libcap2-bin -y

useradd -m ircd

su ircd

(download there the zipfile & unzip there)

autoconf

./configure

make

make install

setcap CAP_NET_BIND_SERVICE=+eip /home/ircd/ircd/bin/ircd
